#!/usr/bin/env bash
SCRIPT="$(readlink -f "$0" 2>/dev/null || python3 -c "import os,sys; print(os.path.realpath(sys.argv[1]))" "$0")"
DIR="$(cd "$(dirname "$SCRIPT")/.." && pwd)"
export RAINDROP_BIN_PATH="${RAINDROP_BIN_PATH:-$SCRIPT}"
exec bun "$DIR/src/index.ts" "$@"
